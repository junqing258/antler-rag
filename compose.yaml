services:
  antler-rag:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      RAG_DATA_DIR: /data
    volumes:
      - rag-data:/data
    restart: unless-stopped

volumes:
  rag-data:
